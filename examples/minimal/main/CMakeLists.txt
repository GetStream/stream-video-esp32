idf_component_register(
    SRCS
        "main.c"
        "app_token.c"
    INCLUDE_DIRS
        "."
    REQUIRES
        stream-video
        nvs_flash
        esp_wifi
        esp_netif
        esp_event
        esp_http_client
        esp_websocket_client
        mbedtls
        json
        codec_board
        esp_psram
)
