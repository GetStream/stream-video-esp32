idf_component_register(
    SRCS
        "main.c"
        "app_token.c"
    INCLUDE_DIRS
        "."
    REQUIRES
        stream-video
        stream-video-capture-default
        nvs_flash
        esp_wifi
        esp_netif
        esp_event
        esp_http_client
        esp_websocket_client
        mbedtls
        json
        esp_capture
        esp_video_codec
        esp_audio_codec
        codec_board
        esp_psram
)
